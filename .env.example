# Prisma SQLite database location (relative to prisma/ dir).
DATABASE_URL="file:./dev.db"

# Session HMAC signing secret — generate with: openssl rand -hex 32
# MUST be set in production. Minimum 32 characters.
SESSION_SECRET=""
